spring.application.name=fhir-backend-microservice
backend.api.enabled=true
mock.fhir.enabled=false
# Comma-separated list of browser origins allowed to call this API.
# Override in Render with app.cors.allowed-origins=https://your-frontend.onrender.com
app.cors.allowed-origins=*
